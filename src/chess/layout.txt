src/cljs =  Client
         -> web


src/clj  = Server
	 -> game (game configuration, client interfaces)
            engines -> ai ->  move_rating    -> materialbewertung
	    	       	  ->  move_selection -> minmax
	    	    -> move_logic (protocols)-> vector
				   	     -> bitmap
	   
	    
